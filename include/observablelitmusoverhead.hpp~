#ifndef OBSERVABLEOVERHEAD_H
#define OBSERVABLEOVERHEAD_H

#include "overheadobserver.hpp"

class ObservableLitmusOverhead {
  
public:
  virtual void setObserver(OverheadObserver*);
  virtual void updateObservers();

};

#endif
