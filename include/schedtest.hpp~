#ifndef SCHEDTEST_H
#define SCHEDTEST_H

#include <cstdlib>
#include <cstdio>

#include "taskset.hpp"
#include "overhead.hpp"
#include "schedtestobserver.hpp"

#define __US_PER_MS 1000

enum SchedTestName {
  DENSITY_TEST,
  BAKERS_TEST
};


class SchedTest: public SchedTestObserver{


  SchedTestName schedTestName;
  TaskSet *taskSet;
  Overhead *overhead;

  int densityTest();
  int get_nbr_cpus();


 public :
  SchedTest();
  ~SchedTest();
  void setTaskSet(TaskSet*);
  void setOverhead(Overhead*);  

  int makeSchedTest();

};


#endif
